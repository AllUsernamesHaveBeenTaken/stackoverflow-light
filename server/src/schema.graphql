# import Question from './generated/prisma.graphql'

type Query {
  feed: Feed!
}

type Mutation {
  askQuestion(title: String!, description: String!): Question!
}

type Feed {
  questions: [Question!]!
  count: Int!
}